name: m2m-passing
title: "M2M Mapping with Passing Tests"
description: |
  A model-to-model mapping with an embedded test suite that passes.
  Exercises PARSE, COMPILE, and TEST_EXECUTION phases of the EMIT runner.

modelSources:
  model:
    root: m2m-passing
    files:
      - model.pure

features:
  - class
  - m2m-mapping
  - mapping
  - data-element
complexity: basic
tags:
  - m2m
  - mapping-test